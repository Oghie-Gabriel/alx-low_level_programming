#include "main.h"

/**
 * set_string - sets the value of a pointer to a char
 * @s: a pointer for a string to be replaced
 * @to: a pointer for a string to be assigned
 *
 * Return: nothing is returned
 */

void set_string(char **s, char *to)
{
*s = to;
}
