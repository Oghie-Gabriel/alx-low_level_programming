#include "main.h"
/**
 * _abs - estimates the absolute value
 * @n: input to be estimates
 * Return: The absolute value of the input
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
